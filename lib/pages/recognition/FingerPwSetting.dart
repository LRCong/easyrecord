import 'package:flutter/material.dart';

class FingerPwSettingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("指纹密码设置"),
      ),
      body: Text("body"),
    );
  }
}
